name: Build and test

on:
  push:

jobs:
  build-and-test:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3

    - name: Restore NuGet packages
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: dotnet restore CalculatorSolution.slnx

    - name: Build
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: dotnet build CalculatorSolution.slnx /p:Configuration=Release /p:Platform=x64

    - name: Tests
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: out/x64/Release/CalculatorTests.exe
